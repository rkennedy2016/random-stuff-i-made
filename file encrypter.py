#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds

#encryption using the ascii code
msg = input("Enter a message to encrypt: ")
encmsg = ""
for ch in msg:
    asc = ord(ch) + 3
    ench = chr(asc)
    encmsg += ench
print("Encrypted message:",encmsg)
import time

time.sleep(5) # Wait for 3 seconds


